[project]
name = "postgres-jsonb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg2-binary>=2.9.12",
    "sqlalchemy>=2.0.49",
]
